import { RoundRealPipe } from './round-real.pipe';

describe('RoundRealPipe', () => {
  it('create an instance', () => {
    const pipe = new RoundRealPipe();
    expect(pipe).toBeTruthy();
  });
});
